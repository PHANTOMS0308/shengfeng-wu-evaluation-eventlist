{
  "dependencies": {
    "express": "^4.18.2",
    "json-server": "^0.17.3"
  },
  "scripts": {
    "database": "npx json-server --watch db.json",
    "server": "npx nodemon index.js"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
